server:
  port: 8080

spring:
  application:
    name: SmartAttendanceSystem

  datasource:
    url: mysql://avnadmin:<redacted>@efssas-db1-efssasdb1.l.aivencloud.com:25853/defaultdb?ssl-mode=REQUIRED
    username: avnadmin
    password: AVNS_hGQnTQjG-lsqgg5Yay_
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

  threads:
    virtual:
      enabled: true

jwt:
  secret: EfsSasSuperSecretKeyForIgnouProjectDoNotShare
  expiration: 36000000
